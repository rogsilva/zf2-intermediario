<h1>Editar</h1>

<br/>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('sonuser-admin/default', array('controller'=>'users', 'action'=>'edit')));
$form->prepare();

echo $this->form()->openTag($form);
echo $this->formCollection($form);
echo $this->form()->closeTag();

?>